<!-- This file is used to markup the public facing widget. -->
<?php 
if( isset( $instance['title'] ) && !empty( $instance['title'] ) )
  echo $before_title.$instance['title'].$after_title;
  
// TODO Build the widget HTLML
?>